🟢 Item.java (simple class)

Create attributes for an item (name, size).
Add a constructor to initialize these values.
toString method that returns the information needed to view the item information.

🟡 Storage.java (storage box logic)

This class has private attributes for the storage box.
a constructor that performs object initialization
a toString method that returns the information of a storage box

method:
create storage box
add items to storage box 
viewing the storage box information

🔵 Main.java (program runner)

Create an array of Storage objects
Show a menu loop: (3 main activities)

1. Add a storage box (ask label + capacity, store in array).
2. Put item into a storage box (ask for item info + storage label, then call addItem).
3. View all storage boxes + their items (print in a table format).

Exit program.
Add input validation 
Keep repeating until user chooses Exit.